import { Stack }  from 'expo-router';

export default function userId() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        >
        </Stack>

    );
    }