import { Stack }  from 'expo-router';

export default function teamId() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        >
        </Stack>

    );
    }